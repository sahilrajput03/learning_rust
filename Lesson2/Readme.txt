Use `cw` or `cargowatch` to execute `cargo run` in watch mode, yikes!

Read about `cargo watch`:
https://crates.io/crates/cargo-watch

Install:
cargo install cargo-watch

Upgrade:
cargo install --force cargo-watch