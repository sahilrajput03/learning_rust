# Using rustmon

rustmon <filename without extension>

For e.g., let say you have a rust program `myprogram.rs` then you'd need to run `rustmon myprogram` from the cli to run it in monitor mode.

# Updating rustc via rustup

```bash
rustup update
```
